// Deduped: this was a divergent second copy (a hardcoded gray-300 skeleton that
// did not theme on the app's dark background). Unified onto the canonical
// design-system spinner at ../LoadingCard.
export * from '../LoadingCard';
