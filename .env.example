# =============================================================================
# GridTokenX Explorer - Configuration Example
# =============================================================================
# Copy this file to .env.local and fill in your values

# Fill these out with a custom RPC url to test the explorer locally without getting rate-limited
# Using public endpoints that support CORS for browser requests
NEXT_PUBLIC_MAINNET_RPC_URL=
NEXT_PUBLIC_DEVNET_RPC_URL=
NEXT_PUBLIC_TESTNET_RPC_URL=

## Local Development RPC (for solana-test-validator)
NEXT_PUBLIC_SOLANA_RPC_HTTP=http://localhost:8899
NEXT_PUBLIC_SOLANA_RPC_WS=ws://localhost:8900

# Network Configuration
# Use 'custom' cluster with local RPC, or switch to devnet/testnet/mainnet-beta
NEXT_PUBLIC_SOLANA_NETWORK=localnet

# =============================================================================
# GridTokenX Program IDs
# =============================================================================
# These IDs should match the program IDs deployed to your local validator
# Update these values after deploying your Anchor programs

# Registry Program - Manages token registry and metadata
NEXT_PUBLIC_REGISTRY_PROGRAM_ID=

# Oracle Program - Provides price feeds and oracle data
NEXT_PUBLIC_ORACLE_PROGRAM_ID=

# Governance Program - Handles DAO governance and voting
NEXT_PUBLIC_GOVERNANCE_PROGRAM_ID=

# Token Program - Custom SPL token implementation
NEXT_PUBLIC_TOKEN_PROGRAM_ID=

# Trading Program - DEX and trading functionality
NEXT_PUBLIC_TRADING_PROGRAM_ID=

# =============================================================================
# API Gateway
# =============================================================================
# GridTokenX API Gateway for backend services
NEXT_PUBLIC_API_GATEWAY_URL=http://localhost:8080

# =============================================================================
# Explorer Configuration
# =============================================================================

## Configuration for "Logger". Set max level of logs to see (0..5)
NEXT_LOG_LEVEL=0

## Configuration for "metadata" service. Set "**_ENABLED=true" to enable
NEXT_PUBLIC_METADATA_ENABLED=false
NEXT_PUBLIC_METADATA_TIMEOUT=
NEXT_PUBLIC_METADATA_MAX_CONTENT_SIZE=
NEXT_PUBLIC_METADATA_USER_AGENT="Solana Explorer"

## Configuration for "Program IDL" feature enabled
NEXT_PUBLIC_PMP_IDL_ENABLED=true

## Configuration for "security.txt" feature enabled
NEXT_PUBLIC_PMP_SECURITY_TXT_ENABLED=true

## Token blacklist (comma-separated addresses)
NEXT_PUBLIC_BAD_TOKENS=

## Configuration for "View CNFT's Original Resource"
NEXT_PUBLIC_VIEW_ORIGINAL_DISPLAY_ENABLED=
